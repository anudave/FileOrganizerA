<UserControl x:Class="WpfApp1.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#2B2B2B">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Application Settings" Foreground="#FF6B35" FontSize="20" FontWeight="Bold" Margin="20,20,20,10"/>

        <!-- Settings Sections -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,10,20,10">

                <!-- General Settings -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="General Settings" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- Theme Selection -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Theme:" Foreground="#CCCCCC" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="DarkThemeRadio" Content="Dark Mode" Foreground="White" IsChecked="True" Margin="0,0,20,0" Click="DarkTheme_Checked"/>
                                <RadioButton x:Name="LightThemeRadio" Content="Light Mode" Foreground="White" Click="LightTheme_Checked"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Default Organization Folder -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Default Organization Folder:" Foreground="#CCCCCC" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="DefaultFolderInput" Grid.Column="0" Background="#2B2B2B" Foreground="White" Padding="8" BorderBrush="#FF6B35" BorderThickness="1" Height="30" AllowDrop="True" DragOver="DefaultFolder_DragOver" Drop="DefaultFolder_Drop"/>
                                <Button Grid.Column="1" Content="Browse" Background="#FF6B35" Foreground="White" Padding="10,0" Click="BrowseDefaultFolder_Click" Margin="10,0,0,0" FontWeight="Bold" Height="30"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Scheduler Settings -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Scheduler Settings" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- Auto-Start Toggle -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <CheckBox x:Name="AutoStartCheckBox" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Start scheduler automatically when app opens" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Notification Settings -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Notification Settings" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- Notifications Toggle -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <CheckBox x:Name="NotificationsCheckBox" IsChecked="True" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Enable notifications for scheduled tasks" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Import/Export -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Data Management" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- Export Options -->
                        <TextBlock Text="Export:" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button Content="Export Rules" Background="#2196F3" Foreground="White" Padding="12,8" Click="ExportRules_Click" Margin="0,0,10,0" FontWeight="Bold" Height="35" Width="130"/>
                            <Button Content="Export Schedules" Background="#2196F3" Foreground="White" Padding="12,8" Click="ExportSchedules_Click" Margin="0,0,10,0" FontWeight="Bold" Height="35" Width="150"/>
                        </StackPanel>

                        <!-- Import Options -->
                        <TextBlock Text="Import:" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button Content="Import Rules" Background="#4CAF50" Foreground="White" Padding="12,8" Click="ImportRules_Click" Margin="0,0,10,0" FontWeight="Bold" Height="35" Width="130"/>
                            <Button Content="Import Schedules" Background="#4CAF50" Foreground="White" Padding="12,8" Click="ImportSchedules_Click" Margin="0,0,10,0" FontWeight="Bold" Height="35" Width="150"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Information -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Application Information" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock Text="Version:" Foreground="#CCCCCC" FontSize="11"/>
                        <TextBlock x:Name="VersionText" Text="1.0.0" Foreground="#FF6B35" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Database Location:" Foreground="#CCCCCC" FontSize="11"/>
                        <TextBlock x:Name="DbLocationText" Text="" Foreground="#FF6B35" FontSize="11" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,10"/>
                        <TextBlock Text="Settings last modified:" Foreground="#CCCCCC" FontSize="11"/>
                        <TextBlock x:Name="LastModifiedText" Text="" Foreground="#FF6B35" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Bottom Buttons -->
        <Border Grid.Row="2" Background="#1E1E1E" Padding="20,10" BorderBrush="#FF6B35" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset to Defaults" Background="#FF9800" Foreground="White" Padding="15,8" Click="ResetDefaults_Click" FontWeight="Bold" Height="40" Width="180" Margin="0,0,10,0"/>
                <Button Content="Save Settings" Background="#4CAF50" Foreground="White" Padding="15,8" Click="SaveSettings_Click" FontWeight="Bold" Height="40" Width="150"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
