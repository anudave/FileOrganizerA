<UserControl x:Class="WpfApp1.Views.CloudOrganizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#2B2B2B">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Cloud Storage Integration" Foreground="#FF6B35" FontSize="20" FontWeight="Bold" Margin="20,20,20,10"/>

        <!-- Connection Status Section -->
        <Border Grid.Row="1" Background="#3B3B3B" Margin="20,10,20,10" Padding="15" CornerRadius="5">
            <StackPanel>
                <!-- Account Status -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Google Drive Account:" Foreground="#CCCCCC" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="AccountStatusText" Text="Not Connected" Foreground="#F44336" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Account Email -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Email:" Foreground="#CCCCCC" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock x:Name="AccountEmailText" Text="—" Foreground="#FF6B35" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="LoginBtn" Content="🔐 Connect Google Drive" Background="#4CAF50" Foreground="White" Padding="15,8" Click="LoginGoogle_Click" FontWeight="Bold" Height="35" Width="200"/>
                    <Button x:Name="LogoutBtn" Content="🔓 Disconnect" Background="#F44336" Foreground="White" Padding="15,8" Click="Logout_Click" FontWeight="Bold" Height="35" Width="150" Margin="10,0,0,0" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,10,20,10">

                <!-- Files Section -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Browse and Organize Cloud Files" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- File List -->
                        <TextBlock Text="Files from Google Drive:" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <DataGrid x:Name="CloudFilesDataGrid" 
                                  Background="#2B2B2B" 
                                  Foreground="White" 
                                  AutoGenerateColumns="False" 
                                  RowBackground="#3B3B3B" 
                                  AlternatingRowBackground="#454545"
                                  BorderBrush="#FF6B35" 
                                  BorderThickness="1"
                                  Height="250">
                            <DataGrid.Columns>
                                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" Width="60"/>
                                <DataGridTextColumn Header="File Name" Binding="{Binding Name}" Width="250" Foreground="White"/>
                                <DataGridTextColumn Header="Size" Binding="{Binding Size, StringFormat='0 B'}" Width="100" Foreground="#CCCCCC"/>
                                <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedTime, StringFormat='yyyy-MM-dd HH:mm'}" Width="150" Foreground="#CCCCCC"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Refresh Button -->
                        <Button Content="🔄 Refresh Files" Background="#2196F3" Foreground="White" Padding="15,8" Click="RefreshFiles_Click" FontWeight="Bold" Height="35" Width="150" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Organization Section -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Organize Selected Files" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- Info -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Selected Files:" Foreground="#CCCCCC" FontSize="11"/>
                            <TextBlock x:Name="SelectedCountText" Text="0" Foreground="#FF6B35" FontSize="11" FontWeight="Bold" Margin="10,0,0,0"/>
                        </StackPanel>

                        <!-- Organization Buttons -->
                        <StackPanel Orientation="Horizontal">
                            <Button Content="🔄 Organize Selected" Background="#4CAF50" Foreground="White" Padding="15,8" Click="OrganizeSelected_Click" FontWeight="Bold" Height="35" Width="200"/>
                            <Button Content="📊 View Results" Background="#2196F3" Foreground="White" Padding="15,8" Click="ViewResults_Click" FontWeight="Bold" Height="35" Width="150" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Results Section -->
                <Border Background="#3B3B3B" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Organization Results" Foreground="#FF6B35" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock x:Name="ResultsText" Foreground="#CCCCCC" FontSize="11" Text="Results will appear here..." TextWrapping="Wrap" MinHeight="100"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#1E1E1E" Padding="20,10" BorderBrush="#FF6B35" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusText" Foreground="#CCCCCC" FontSize="11" Text="Ready"/>
                <TextBlock Text=" | " Foreground="#666666" Margin="10,0"/>
                <TextBlock x:Name="LastSyncText" Foreground="#CCCCCC" FontSize="11" Text="Last Sync: Never"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
