<UserControl x:Class="WpfApp1.Views.FileOrganizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#2B2B2B">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Folder Selection &amp; Organization" Foreground="#FF6B35" FontSize="20" FontWeight="Bold" Margin="20,20,20,10"/>

        <!-- Content -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Header -->
            <TextBlock Text="Folder Selection (Drop Zone)" Foreground="#FF6B35" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Drop Zone -->
            <Border x:Name="DropZone" 
                    BorderThickness="2" 
                    BorderBrush="#FF6B35" 
                    Background="#3B3B3B" 
                    CornerRadius="10" 
                    Height="250" 
                    Width="600"
                    AllowDrop="True">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- Folder Icon -->
                    <TextBlock Text="📁" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="Drag &amp; Drop Folder Here" Foreground="White" FontSize="16" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                    <TextBlock x:Name="FolderPathText" Text="Select a folder..." Foreground="#999999" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <Button Content="Browse Folder" Background="#FF6B35" Foreground="White" Padding="15,8" Margin="0,20,0,0" HorizontalAlignment="Center" Width="150" Height="40" Click="BrowseFolder_Click"/>
                </StackPanel>
            </Border>

            <!-- File Statistics -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Total Files: " Foreground="White" FontSize="12"/>
                <TextBlock x:Name="TotalFilesText" Text="0" Foreground="#FF6B35" FontSize="12" FontWeight="Bold"/>
                <TextBlock Text=" | Size: " Foreground="White" FontSize="12" Margin="20,0,0,0"/>
                <TextBlock x:Name="TotalSizeText" Text="0 B" Foreground="#FF6B35" FontSize="12" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
