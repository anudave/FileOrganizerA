<UserControl x:Class="WpfApp1.Views.RuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#2B2B2B">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="File Organization Rules" Foreground="#FF6B35" FontSize="20" FontWeight="Bold" Margin="20,20,20,10"/>

        <!-- Add Rule Section -->
        <Border Grid.Row="1" Background="#3B3B3B" Margin="20,10,20,10" Padding="15" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Create New Rule" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Rule Name -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Rule Name:" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="RuleNameInput" Background="#2B2B2B" Foreground="White" Padding="8" BorderBrush="#FF6B35" BorderThickness="1"/>
                    </StackPanel>

                    <!-- File Pattern -->
                    <StackPanel Grid.Column="1" Margin="10,0,10,0">
                        <TextBlock Text="File Pattern:" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="FilePatternInput" Background="#2B2B2B" Foreground="White" Padding="8" BorderBrush="#FF6B35" BorderThickness="1" Text="*.pdf"/>
                    </StackPanel>

                    <!-- Destination Folder -->
                    <StackPanel Grid.Column="2" Margin="10,0,10,0">
                        <TextBlock Text="Destination Folder:" Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="DestinationFolderInput" Background="#2B2B2B" Foreground="White" Padding="8" BorderBrush="#FF6B35" BorderThickness="1"/>
                    </StackPanel>

                    <!-- Add Button -->
                    <Button Grid.Column="3" Content="+ Add Rule" Background="#FF6B35" Foreground="White" Padding="15,8" Margin="10,0,0,0" VerticalAlignment="Bottom" Height="35" Click="AddRule_Click" FontWeight="Bold"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Rules List -->
        <DataGrid Grid.Row="2" x:Name="RulesDataGrid" Background="#2B2B2B" Foreground="White" Margin="20" 
                  AutoGenerateColumns="False" RowBackground="#3B3B3B" 
                  AlternatingRowBackground="#454545" BorderBrush="#FF6B35" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True" Foreground="White"/>
                <DataGridTextColumn Header="Rule Name" Binding="{Binding RuleName}" Width="*" Foreground="White"/>
                <DataGridTextColumn Header="File Pattern" Binding="{Binding FilePattern}" Width="120" Foreground="White"/>
                <DataGridTextColumn Header="Destination Folder" Binding="{Binding DestinationFolder}" Width="200" Foreground="White"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70"/>
                <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="150" IsReadOnly="True" Foreground="White"/>
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Background="#4CAF50" Foreground="White" Padding="8,4" Margin="0,0,5,0" Click="EditRule_Click" Tag="{Binding Id}"/>
                                <Button Content="Delete" Background="#F44336" Foreground="White" Padding="8,4" Click="DeleteRule_Click" Tag="{Binding Id}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#1E1E1E" Padding="20,10" BorderBrush="#FF6B35" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Foreground="#CCCCCC" FontSize="12" Text="Ready"/>
        </Border>
    </Grid>
</UserControl>
